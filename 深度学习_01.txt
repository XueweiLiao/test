线性模型
	梯度下降法
		往负梯度方向
	主要工作是 调整 w 
	a 是非常重要的超参数
		不能太大，也不能太小
	求导，求偏导，
		x 偏导， y 偏导， 两者夹角为向量
	
图像处理
	1.非黑即白 0/1
		每个像素点都是一个位
	2.灰度图像 0/255
		每个像素点是一个字节（8位）
	3.彩色图像 （r,g,b）
		每个像素点都是一个向量 
		
		
主要任务
	图像分类（最基础）
		对大量图片进行分类
	语义分割
		对图片进行分析和转换
	实例分割
		与语义分割结合使用

深度学习
	将高维向量抛入特定函数中，寻找参数
	有真实值，预测值，损失值，
	将真实值代入损失函数，求得损失值，
	利用梯度下降法，降低损失值，重新计算参数，
	循环

f(x)=w`X

损失函数
	合页损失函数
优化算法
	小批量梯度下降
		将大量数据均分，每次顺序取一部分
	随机梯度下降
		每次随机取大量数据的一部分

图像载入
	PIL =》RGB    0~255 utf-8   【H W C】  from PIL import Image
	OpenCV =》BGR   0~255 utf-8   【H W C】	import cv2
	Tensor	RGB   0~1 float32   【C H W】  import torch